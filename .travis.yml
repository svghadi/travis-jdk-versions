jobs:
  include:
    - arch: amd64
      dist: bionic
    - arch: amd64
      dist: focal
    - arch: s390x
      dist: bionic
    - arch: ppc64le
      dist: xenial
    - arch: ppc64le
      dist: bionic
    - arch: ppc64le
      dist: focal

language: java
jdk: openjdk8

script:
  - java -version

