jobs:
  include:
    # power jobs  
    - os: linux-ppc64le
      dist: xenial
    - arch: ppc64le
      dist: bionic
    - arch: ppc64le
      dist: focal

language: java
jdk: openjdk11

script:
  - env
  - java -version
  - mvn --version
