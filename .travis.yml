jobs:
  include:
    # power jobs  
    - arch: ppc64le
      dist: xenial
    - arch: ppc64le
      dist: bionic
    - arch: ppc64le
      dist: focal

language: java
jdk: openjdk11

script:
  - mvn --version
